[project]
name = "modern-gcp-medallion-dbt-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-athena>=1.10.0",
]
